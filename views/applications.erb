<html>
<head>
	<title></title>
	<link href="/table.css" rel="stylesheet" type="text/css" />
	<link href="/style.css" rel="stylesheet" type="text/css" />
</head>
	<body>
		<div id="container">
			
			<%= erb :header %>

			<table class="table_app">
			<thead>
			<tr>
				<th>Дете</th>
				<th>Училище</th>
				<th>Пореден номер</th>
				<th>Дата</th>
				<th>&nbsp;</th>
			</tr>
			</thead>
			<tbody>
				<% @applications.each do |app| %>
				<tr>
					<td><%= app.child.to_s %></td>
					<td><%= app.school.name %></td>
					<td><%= app.num %></td>
					<td><%= app.created_at %></td>
					<td>
						<form action="/application/<%= app.id %>" method="POST">
							<input type="hidden" name="_method" value="delete">
	    					<input type="submit" value="Изтрий">
						</form>
					</td>
				</tr>
				<% end %>
			</tbody>
			</table>
			<form action="/applications/new" class="link">
				<input type="submit"value="Добавяне на кандидатура"/>
			</form>
		</div>
	</body>
</html>

